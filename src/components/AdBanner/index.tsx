import React from 'react';

import { Container } from './styles';

const AdBanner: React.FC = () => {
  return (
    <Container className="ad-banner">
      <span>Ui Clone - </span>
      interface feita para aprimorar conhecimentos
    </Container>
  );
};

export default AdBanner;
